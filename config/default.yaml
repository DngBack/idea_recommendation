# =============================================
# Idea Generator – Default Configuration
# =============================================
# Override any of these via CLI flags, e.g.:
#   idea-generator --topic-file topic.md --model gpt-4o --max-generations 10

# LLM model to use for idea generation
# Examples: gpt-4o-2024-05-13, gpt-5.2 (thinking), gpt-5.2-pro, claude-3-5-sonnet-20241022
model: "gpt-5.2"

# Maximum number of ideas to generate in one run
max_generations: 3

# Number of reflection / refinement rounds per idea
num_reflections: 5

# Directory for output files (relative to CWD)
output_dir: "output"

# Validate each idea against JSON schema before accepting it
validate: true

# Enable novelty scoring (uses an extra LLM call per idea)
novelty_scoring: false

# Model to use for novelty scoring (empty = same as `model`)
novelty_model: ""

# How often to write a checkpoint (every N ideas)
checkpoint_interval: 1

# Search tools (no S2 key required by default: use arXiv + Tavily)
s2_enabled: false
arxiv_enabled: true
tavily_enabled: true

# Enable PubMed search (biology, medicine, NLP)
pubmed_enabled: false

# Enable OpenAlex search (broad coverage, DOI, citations)
openalex_enabled: false

# Resume from a previous checkpoint (set to true or use --resume flag)
resume: false

# Override the system prompt entirely (leave empty to use the built-in prompt)
system_prompt_override: ""

# Research pipeline: lit review -> hypotheses -> feasibility selection -> direction -> experiment plan -> critique
# Use --pipeline to run full pipeline, or --phase <name> with --from-* for a single phase
# --skip-feasibility / --skip-critique to omit those steps in full pipeline
pipeline_mode: false
research_pipeline:
  # Literature review: number of rounds; each round = one LLM turn (search or finalize). Use 12+ for a detailed review with many searches.
  literature_reflections: 3
  direction_reflections: 3
  max_hypotheses: 1
  skip_feasibility: false
  skip_critique: false
  # Use OpenAI Structured Outputs (response_format json_schema) when model supports it (gpt-4o, gpt-4.1, etc.). Reduces JSON parse failures.
  structured_output: true
  # Critique personas: list of ids to run, or omit for all. Options: icml_reviewer, neurips_reviewer, senior_professor, skeptic_reviewer
  critique_persona_ids: null
